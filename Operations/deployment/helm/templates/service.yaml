apiVersion: v1
kind: Service
metadata:
  name: {{ include "daioph.fullname" . }}
  labels:
    {{- include "daioph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daioph.selectorLabels" . | nindent 4 }}